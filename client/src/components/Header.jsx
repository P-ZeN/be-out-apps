import React from "react";
import MainMenu from "./MainMenu";

const Header = () => {
    return (
        <header>
            <MainMenu />
            <h1>Be-Out App</h1>
        </header>
    );
};

export default Header;
